const expect = require("chai").expect;
const center = require("../modules/centerOfArray");

describe("A module algorithm", function() {
  it("Will have a value", function() {
    expect(center.value).to.be.true;
  });
});
